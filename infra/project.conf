server {
    listen       80 default_server;
    listen       [::]:80 default_server;
    server_name  _;
    server_tokens off;

    location /api/ {
        proxy_pass http://auth-service:8000;
    }
}